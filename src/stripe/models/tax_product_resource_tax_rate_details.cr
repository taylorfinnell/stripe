#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2025-06-30.basil
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"

require "./tax_rate_flat_amount" 

module Stripe
  # 
  class TaxProductResourceTaxRateDetails
    include JSON::Serializable
    include JSON::Serializable::Unmapped
    include OpenApi::Validatable
    include OpenApi::Json

    # Required Properties

    # The tax rate percentage as a string. For example, 8.5% is represented as `\"8.5\"`.
    @[JSON::Field(key: "percentage_decimal", type: String?, default: nil, required: true, nullable: false, emit_null: false)]
    getter percentage_decimal : String? = nil
    MAX_LENGTH_FOR_PERCENTAGE_DECIMAL = 5000


    # End of Required Properties

    # Optional Properties

    # Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
    @[JSON::Field(key: "country", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: country.nil? && !country_present?)]
    getter country : String? = nil
    MAX_LENGTH_FOR_COUNTRY = 5000

    @[JSON::Field(ignore: true)]
    property? country_present : Bool = false


    @[JSON::Field(key: "flat_amount", type: Stripe::TaxRateFlatAmount?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: flat_amount.nil? && !flat_amount_present?)]
    getter flat_amount : Stripe::TaxRateFlatAmount? = nil

    @[JSON::Field(ignore: true)]
    property? flat_amount_present : Bool = false


    # Indicates the type of tax rate applied to the taxable amount. This value can be `null` when no tax applies to the location. This field is only present for TaxRates created by Stripe Tax.
    @[JSON::Field(key: "rate_type", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: rate_type.nil? && !rate_type_present?)]
    getter rate_type : String? = nil
    ERROR_MESSAGE_FOR_RATE_TYPE = "invalid value for \"rate_type\", must be one of [flat_amount, percentage]."
    VALID_VALUES_FOR_RATE_TYPE = String.static_array("flat_amount", "percentage")

    @[JSON::Field(ignore: true)]
    property? rate_type_present : Bool = false


    # State, county, province, or region.
    @[JSON::Field(key: "state", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: state.nil? && !state_present?)]
    getter state : String? = nil
    MAX_LENGTH_FOR_STATE = 5000

    @[JSON::Field(ignore: true)]
    property? state_present : Bool = false


    # The tax type, such as `vat` or `sales_tax`.
    @[JSON::Field(key: "tax_type", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: tax_type.nil? && !tax_type_present?)]
    getter tax_type : String? = nil
    ERROR_MESSAGE_FOR_TAX_TYPE = "invalid value for \"tax_type\", must be one of [amusement_tax, communications_tax, gst, hst, igst, jct, lease_tax, pst, qst, retail_delivery_fee, rst, sales_tax, service_tax, vat]."
    VALID_VALUES_FOR_TAX_TYPE = String.static_array("amusement_tax", "communications_tax", "gst", "hst", "igst", "jct", "lease_tax", "pst", "qst", "retail_delivery_fee", "rst", "sales_tax", "service_tax", "vat")

    @[JSON::Field(ignore: true)]
    property? tax_type_present : Bool = false


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(
      *,
      # Required properties
      @percentage_decimal : String? = nil,
      # Optional properties
      @country : String? = nil,
      @flat_amount : Stripe::TaxRateFlatAmount? = nil,
      @rate_type : String? = nil,
      @state : String? = nil,
      @tax_type : String? = nil
    )
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties : Array(String)
      invalid_properties = Array(String).new

      invalid_properties.push("\"percentage_decimal\" is required and cannot be null") if @percentage_decimal.nil?

      unless (_percentage_decimal = @percentage_decimal).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("percentage_decimal", _percentage_decimal.to_s.size, MAX_LENGTH_FOR_PERCENTAGE_DECIMAL)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_country = @country).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("country", _country.to_s.size, MAX_LENGTH_FOR_COUNTRY)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_flat_amount = @flat_amount).nil?
        invalid_properties.concat(_flat_amount.list_invalid_properties_for("flat_amount")) if _flat_amount.is_a?(OpenApi::Validatable)
      end
      unless (_rate_type = @rate_type).nil?
        invalid_properties.push(ERROR_MESSAGE_FOR_RATE_TYPE) unless OpenApi::EnumValidator.valid?(_rate_type, VALID_VALUES_FOR_RATE_TYPE)
      end
      unless (_state = @state).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("state", _state.to_s.size, MAX_LENGTH_FOR_STATE)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_tax_type = @tax_type).nil?
        invalid_properties.push(ERROR_MESSAGE_FOR_TAX_TYPE) unless OpenApi::EnumValidator.valid?(_tax_type, VALID_VALUES_FOR_TAX_TYPE)
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid? : Bool

      return false if @percentage_decimal.nil?
      unless (_percentage_decimal = @percentage_decimal).nil?
        return false if _percentage_decimal.to_s.size > MAX_LENGTH_FOR_PERCENTAGE_DECIMAL
      end

      unless (_country = @country).nil?
        return false if _country.to_s.size > MAX_LENGTH_FOR_COUNTRY
      end

      unless (_flat_amount = @flat_amount).nil?
        return false if _flat_amount.is_a?(OpenApi::Validatable) && !_flat_amount.valid?
      end

      unless (_rate_type = @rate_type).nil?
        return false unless OpenApi::EnumValidator.valid?(_rate_type, VALID_VALUES_FOR_RATE_TYPE)
      end

      unless (_state = @state).nil?
        return false if _state.to_s.size > MAX_LENGTH_FOR_STATE
      end

      unless (_tax_type = @tax_type).nil?
        return false unless OpenApi::EnumValidator.valid?(_tax_type, VALID_VALUES_FOR_TAX_TYPE)
      end

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] percentage_decimal Object to be assigned
    def percentage_decimal=(new_value : String?)

      raise ArgumentError.new("\"percentage_decimal\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("percentage_decimal", new_value.to_s.size, MAX_LENGTH_FOR_PERCENTAGE_DECIMAL)
      end

      @percentage_decimal = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] country Object to be assigned
    def country=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("country", new_value.to_s.size, MAX_LENGTH_FOR_COUNTRY)
      end

      @country = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] flat_amount Object to be assigned
    def flat_amount=(new_value : Stripe::TaxRateFlatAmount?)

      unless new_value.nil?
        new_value.validate if new_value.is_a?(OpenApi::Validatable)
      end

      @flat_amount = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] rate_type Object to be assigned
    def rate_type=(new_value : String?)

      unless new_value.nil?
        OpenApi::EnumValidator.validate("rate_type", new_value, VALID_VALUES_FOR_RATE_TYPE)
      end

      @rate_type = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] state Object to be assigned
    def state=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("state", new_value.to_s.size, MAX_LENGTH_FOR_STATE)
      end

      @state = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] tax_type Object to be assigned
    def tax_type=(new_value : String?)

      unless new_value.nil?
        OpenApi::EnumValidator.validate("tax_type", new_value, VALID_VALUES_FOR_TAX_TYPE)
      end

      @tax_type = new_value
    end

    # Generates #hash and #== methods from all fields
    # #== @return [Bool]
    # #hash calculates hash code according to all attributes.
    # #hash @return [UInt64] Hash code
    def_equals_and_hash(@percentage_decimal, @country, @country_present, @flat_amount, @flat_amount_present, @rate_type, @rate_type_present, @state, @state_present, @tax_type, @tax_type_present)
  end
end
