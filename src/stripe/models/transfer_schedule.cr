#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2025-06-30.basil
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"


module Stripe
  # 
  class TransferSchedule
    include JSON::Serializable
    include JSON::Serializable::Unmapped
    include OpenApi::Validatable
    include OpenApi::Json

    # Required Properties

    # The number of days charges for the account will be held before being paid out.
    @[JSON::Field(key: "delay_days", type: Int32?, default: nil, required: true, nullable: false, emit_null: false)]
    getter delay_days : Int32? = nil



    # How frequently funds will be paid out. One of `manual` (payouts only created via API call), `daily`, `weekly`, or `monthly`.
    @[JSON::Field(key: "interval", type: String?, default: nil, required: true, nullable: false, emit_null: false)]
    getter interval : String? = nil
    MAX_LENGTH_FOR_INTERVAL = 5000


    # End of Required Properties

    # Optional Properties

    # The day of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled between the 29th and 31st of the month are sent on the last day of shorter months.
    @[JSON::Field(key: "monthly_anchor", type: Int32?, default: nil, required: false, nullable: false, emit_null: false)]
    getter monthly_anchor : Int32? = nil



    # The days of the month funds will be paid out. Only shown if `interval` is monthly. Payouts scheduled between the 29th and 31st of the month are sent on the last day of shorter months.
    @[JSON::Field(key: "monthly_payout_days", type: Array(Int32)?, default: nil, required: false, nullable: false, emit_null: false)]
    getter monthly_payout_days : Array(Int32)? = nil



    # The day of the week funds will be paid out, of the style 'monday', 'tuesday', etc. Only shown if `interval` is weekly.
    @[JSON::Field(key: "weekly_anchor", type: String?, default: nil, required: false, nullable: false, emit_null: false)]
    getter weekly_anchor : String? = nil
    MAX_LENGTH_FOR_WEEKLY_ANCHOR = 5000



    # The days of the week when available funds are paid out, specified as an array, for example, [`monday`, `tuesday`]. Only shown if `interval` is weekly.
    @[JSON::Field(key: "weekly_payout_days", type: Array(String)?, default: nil, required: false, nullable: false, emit_null: false)]
    getter weekly_payout_days : Array(String)? = nil
    ERROR_MESSAGE_FOR_WEEKLY_PAYOUT_DAYS = "invalid value for \"weekly_payout_days\", must be one of [friday, monday, saturday, sunday, thursday, tuesday, wednesday]."
    VALID_VALUES_FOR_WEEKLY_PAYOUT_DAYS = String.static_array("friday", "monday", "saturday", "sunday", "thursday", "tuesday", "wednesday")



    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(
      *,
      # Required properties
      @delay_days : Int32? = nil, 
      @interval : String? = nil,
      # Optional properties
      @monthly_anchor : Int32? = nil,
      @monthly_payout_days : Array(Int32)? = nil,
      @weekly_anchor : String? = nil,
      @weekly_payout_days : Array(String)? = nil
    )
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties : Array(String)
      invalid_properties = Array(String).new

      invalid_properties.push("\"delay_days\" is required and cannot be null") if @delay_days.nil?

      unless (_delay_days = @delay_days).nil?
      end
      invalid_properties.push("\"interval\" is required and cannot be null") if @interval.nil?

      unless (_interval = @interval).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("interval", _interval.to_s.size, MAX_LENGTH_FOR_INTERVAL)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_monthly_anchor = @monthly_anchor).nil?
      end
      unless (_monthly_payout_days = @monthly_payout_days).nil?
      end
      unless (_weekly_anchor = @weekly_anchor).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("weekly_anchor", _weekly_anchor.to_s.size, MAX_LENGTH_FOR_WEEKLY_ANCHOR)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_weekly_payout_days = @weekly_payout_days).nil?
        invalid_properties.push(ERROR_MESSAGE_FOR_WEEKLY_PAYOUT_DAYS) unless OpenApi::EnumValidator.valid?(_weekly_payout_days, VALID_VALUES_FOR_WEEKLY_PAYOUT_DAYS)
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid? : Bool

      return false if @delay_days.nil?
      unless (_delay_days = @delay_days).nil?
      end

      return false if @interval.nil?
      unless (_interval = @interval).nil?
        return false if _interval.to_s.size > MAX_LENGTH_FOR_INTERVAL
      end

      unless (_monthly_anchor = @monthly_anchor).nil?
      end

      unless (_monthly_payout_days = @monthly_payout_days).nil?
      end

      unless (_weekly_anchor = @weekly_anchor).nil?
        return false if _weekly_anchor.to_s.size > MAX_LENGTH_FOR_WEEKLY_ANCHOR
      end

      unless (_weekly_payout_days = @weekly_payout_days).nil?
        return false unless OpenApi::EnumValidator.valid?(_weekly_payout_days, VALID_VALUES_FOR_WEEKLY_PAYOUT_DAYS)
      end

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] delay_days Object to be assigned
    def delay_days=(new_value : Int32?)

      raise ArgumentError.new("\"delay_days\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
      end

      @delay_days = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] interval Object to be assigned
    def interval=(new_value : String?)

      raise ArgumentError.new("\"interval\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("interval", new_value.to_s.size, MAX_LENGTH_FOR_INTERVAL)
      end

      @interval = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] monthly_anchor Object to be assigned
    def monthly_anchor=(new_value : Int32?)

      unless new_value.nil?
      end

      @monthly_anchor = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] monthly_payout_days Object to be assigned
    def monthly_payout_days=(new_value : Array(Int32)?)

      unless new_value.nil?
      end

      @monthly_payout_days = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] weekly_anchor Object to be assigned
    def weekly_anchor=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("weekly_anchor", new_value.to_s.size, MAX_LENGTH_FOR_WEEKLY_ANCHOR)
      end

      @weekly_anchor = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] weekly_payout_days Object to be assigned
    def weekly_payout_days=(new_value : Array(String)?)

      unless new_value.nil?
        OpenApi::EnumValidator.validate("weekly_payout_days", new_value, VALID_VALUES_FOR_WEEKLY_PAYOUT_DAYS)
      end

      @weekly_payout_days = new_value
    end

    # Generates #hash and #== methods from all fields
    # #== @return [Bool]
    # #hash calculates hash code according to all attributes.
    # #hash @return [UInt64] Hash code
    def_equals_and_hash(@delay_days, @interval, @monthly_anchor, @monthly_payout_days, @weekly_anchor, @weekly_payout_days)
  end
end
