#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2025-06-30.basil
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"

require "./terminal_reader_reader_resource_choice" 

module Stripe
  # Information about a selection being collected using a reader
  class TerminalReaderReaderResourceSelection
    include JSON::Serializable
    include JSON::Serializable::Unmapped
    include OpenApi::Validatable
    include OpenApi::Json

    # Required Properties

    # List of possible choices to be selected
    @[JSON::Field(key: "choices", type: Array(Stripe::TerminalReaderReaderResourceChoice)?, default: nil, required: true, nullable: false, emit_null: false)]
    getter choices : Array(Stripe::TerminalReaderReaderResourceChoice)? = nil


    # End of Required Properties

    # Optional Properties

    # The id of the selected choice
    @[JSON::Field(key: "id", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: id.nil? && !id_present?)]
    getter id : String? = nil
    MAX_LENGTH_FOR_ID = 5000

    @[JSON::Field(ignore: true)]
    property? id_present : Bool = false


    # The text of the selected choice
    @[JSON::Field(key: "text", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: text.nil? && !text_present?)]
    getter text : String? = nil
    MAX_LENGTH_FOR_TEXT = 5000

    @[JSON::Field(ignore: true)]
    property? text_present : Bool = false


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(
      *,
      # Required properties
      @choices : Array(Stripe::TerminalReaderReaderResourceChoice)? = nil,
      # Optional properties
      @id : String? = nil,
      @text : String? = nil
    )
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties : Array(String)
      invalid_properties = Array(String).new

      invalid_properties.push("\"choices\" is required and cannot be null") if @choices.nil?

      unless (_choices = @choices).nil?
        invalid_properties.concat(OpenApi::ContainerValidator.list_invalid_properties_for(key: "choices", container: _choices)) if _choices.is_a?(Array)
      end
      unless (_id = @id).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("id", _id.to_s.size, MAX_LENGTH_FOR_ID)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_text = @text).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("text", _text.to_s.size, MAX_LENGTH_FOR_TEXT)
          invalid_properties.push(max_length_error)
        end
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid? : Bool

      return false if @choices.nil?
      unless (_choices = @choices).nil?
        return false if _choices.is_a?(Array) && !OpenApi::ContainerValidator.valid?(container: _choices)
      end

      unless (_id = @id).nil?
        return false if _id.to_s.size > MAX_LENGTH_FOR_ID
      end

      unless (_text = @text).nil?
        return false if _text.to_s.size > MAX_LENGTH_FOR_TEXT
      end

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] choices Object to be assigned
    def choices=(new_value : Array(Stripe::TerminalReaderReaderResourceChoice)?)

      raise ArgumentError.new("\"choices\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
        OpenApi::ContainerValidator.validate(container: new_value) if new_value.is_a?(Array)
      end

      @choices = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] id Object to be assigned
    def id=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("id", new_value.to_s.size, MAX_LENGTH_FOR_ID)
      end

      @id = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] text Object to be assigned
    def text=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("text", new_value.to_s.size, MAX_LENGTH_FOR_TEXT)
      end

      @text = new_value
    end

    # Generates #hash and #== methods from all fields
    # #== @return [Bool]
    # #hash calculates hash code according to all attributes.
    # #hash @return [UInt64] Hash code
    def_equals_and_hash(@choices, @id, @id_present, @text, @text_present)
  end
end
