#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2025-06-30.basil
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"

require "./account_requirements_alternative" 
require "./account_requirements_error" 

module Stripe
  # 
  class AccountFutureRequirements
    include JSON::Serializable
    include JSON::Serializable::Unmapped
    include OpenApi::Validatable
    include OpenApi::Json


    # Optional Properties

    # Fields that are due and can be satisfied by providing the corresponding alternative fields instead.
    @[JSON::Field(key: "alternatives", type: Array(Stripe::AccountRequirementsAlternative)?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: alternatives.nil? && !alternatives_present?)]
    getter alternatives : Array(Stripe::AccountRequirementsAlternative)? = nil

    @[JSON::Field(ignore: true)]
    property? alternatives_present : Bool = false


    # Date on which `future_requirements` becomes the main `requirements` hash and `future_requirements` becomes empty. After the transition, `currently_due` requirements may immediately become `past_due`, but the account may also be given a grace period depending on its enablement state prior to transitioning.
    @[JSON::Field(key: "current_deadline", type: Int32?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: current_deadline.nil? && !current_deadline_present?)]
    getter current_deadline : Int32? = nil

    @[JSON::Field(ignore: true)]
    property? current_deadline_present : Bool = false


    # Fields that need to be collected to keep the account enabled. If not collected by `future_requirements[current_deadline]`, these fields will transition to the main `requirements` hash.
    @[JSON::Field(key: "currently_due", type: Array(String)?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: currently_due.nil? && !currently_due_present?)]
    getter currently_due : Array(String)? = nil

    @[JSON::Field(ignore: true)]
    property? currently_due_present : Bool = false


    # This is typed as an enum for consistency with `requirements.disabled_reason`.
    @[JSON::Field(key: "disabled_reason", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: disabled_reason.nil? && !disabled_reason_present?)]
    getter disabled_reason : String? = nil
    ERROR_MESSAGE_FOR_DISABLED_REASON = "invalid value for \"disabled_reason\", must be one of [action_required.requested_capabilities, listed, other, platform_paused, rejected.fraud, rejected.incomplete_verification, rejected.listed, rejected.other, rejected.platform_fraud, rejected.platform_other, rejected.platform_terms_of_service, rejected.terms_of_service, requirements.past_due, requirements.pending_verification, under_review]."
    VALID_VALUES_FOR_DISABLED_REASON = String.static_array("action_required.requested_capabilities", "listed", "other", "platform_paused", "rejected.fraud", "rejected.incomplete_verification", "rejected.listed", "rejected.other", "rejected.platform_fraud", "rejected.platform_other", "rejected.platform_terms_of_service", "rejected.terms_of_service", "requirements.past_due", "requirements.pending_verification", "under_review")

    @[JSON::Field(ignore: true)]
    property? disabled_reason_present : Bool = false


    # Fields that are `currently_due` and need to be collected again because validation or verification failed.
    @[JSON::Field(key: "errors", type: Array(Stripe::AccountRequirementsError)?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: errors.nil? && !errors_present?)]
    getter errors : Array(Stripe::AccountRequirementsError)? = nil

    @[JSON::Field(ignore: true)]
    property? errors_present : Bool = false


    # Fields you must collect when all thresholds are reached. As they become required, they appear in `currently_due` as well.
    @[JSON::Field(key: "eventually_due", type: Array(String)?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: eventually_due.nil? && !eventually_due_present?)]
    getter eventually_due : Array(String)? = nil

    @[JSON::Field(ignore: true)]
    property? eventually_due_present : Bool = false


    # Fields that weren't collected by `requirements.current_deadline`. These fields need to be collected to enable the capability on the account. New fields will never appear here; `future_requirements.past_due` will always be a subset of `requirements.past_due`.
    @[JSON::Field(key: "past_due", type: Array(String)?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: past_due.nil? && !past_due_present?)]
    getter past_due : Array(String)? = nil

    @[JSON::Field(ignore: true)]
    property? past_due_present : Bool = false


    # Fields that might become required depending on the results of verification or review. It's an empty array unless an asynchronous verification is pending. If verification fails, these fields move to `eventually_due` or `currently_due`. Fields might appear in `eventually_due` or `currently_due` and in `pending_verification` if verification fails but another verification is still pending.
    @[JSON::Field(key: "pending_verification", type: Array(String)?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: pending_verification.nil? && !pending_verification_present?)]
    getter pending_verification : Array(String)? = nil

    @[JSON::Field(ignore: true)]
    property? pending_verification_present : Bool = false


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(
      *,
      # Optional properties
      @alternatives : Array(Stripe::AccountRequirementsAlternative)? = nil,
      @current_deadline : Int32? = nil,
      @currently_due : Array(String)? = nil,
      @disabled_reason : String? = nil,
      @errors : Array(Stripe::AccountRequirementsError)? = nil,
      @eventually_due : Array(String)? = nil,
      @past_due : Array(String)? = nil,
      @pending_verification : Array(String)? = nil
    )
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties : Array(String)
      invalid_properties = Array(String).new

      unless (_alternatives = @alternatives).nil?
        invalid_properties.concat(OpenApi::ContainerValidator.list_invalid_properties_for(key: "alternatives", container: _alternatives)) if _alternatives.is_a?(Array)
      end
      unless (_current_deadline = @current_deadline).nil?
      end
      unless (_currently_due = @currently_due).nil?
      end
      unless (_disabled_reason = @disabled_reason).nil?
        invalid_properties.push(ERROR_MESSAGE_FOR_DISABLED_REASON) unless OpenApi::EnumValidator.valid?(_disabled_reason, VALID_VALUES_FOR_DISABLED_REASON)
      end
      unless (_errors = @errors).nil?
        invalid_properties.concat(OpenApi::ContainerValidator.list_invalid_properties_for(key: "errors", container: _errors)) if _errors.is_a?(Array)
      end
      unless (_eventually_due = @eventually_due).nil?
      end
      unless (_past_due = @past_due).nil?
      end
      unless (_pending_verification = @pending_verification).nil?
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid? : Bool

      unless (_alternatives = @alternatives).nil?
        return false if _alternatives.is_a?(Array) && !OpenApi::ContainerValidator.valid?(container: _alternatives)
      end

      unless (_current_deadline = @current_deadline).nil?
      end

      unless (_currently_due = @currently_due).nil?
      end

      unless (_disabled_reason = @disabled_reason).nil?
        return false unless OpenApi::EnumValidator.valid?(_disabled_reason, VALID_VALUES_FOR_DISABLED_REASON)
      end

      unless (_errors = @errors).nil?
        return false if _errors.is_a?(Array) && !OpenApi::ContainerValidator.valid?(container: _errors)
      end

      unless (_eventually_due = @eventually_due).nil?
      end

      unless (_past_due = @past_due).nil?
      end

      unless (_pending_verification = @pending_verification).nil?
      end

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] alternatives Object to be assigned
    def alternatives=(new_value : Array(Stripe::AccountRequirementsAlternative)?)

      unless new_value.nil?
        OpenApi::ContainerValidator.validate(container: new_value) if new_value.is_a?(Array)
      end

      @alternatives = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] current_deadline Object to be assigned
    def current_deadline=(new_value : Int32?)

      unless new_value.nil?
      end

      @current_deadline = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] currently_due Object to be assigned
    def currently_due=(new_value : Array(String)?)

      unless new_value.nil?
      end

      @currently_due = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] disabled_reason Object to be assigned
    def disabled_reason=(new_value : String?)

      unless new_value.nil?
        OpenApi::EnumValidator.validate("disabled_reason", new_value, VALID_VALUES_FOR_DISABLED_REASON)
      end

      @disabled_reason = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] errors Object to be assigned
    def errors=(new_value : Array(Stripe::AccountRequirementsError)?)

      unless new_value.nil?
        OpenApi::ContainerValidator.validate(container: new_value) if new_value.is_a?(Array)
      end

      @errors = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] eventually_due Object to be assigned
    def eventually_due=(new_value : Array(String)?)

      unless new_value.nil?
      end

      @eventually_due = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] past_due Object to be assigned
    def past_due=(new_value : Array(String)?)

      unless new_value.nil?
      end

      @past_due = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] pending_verification Object to be assigned
    def pending_verification=(new_value : Array(String)?)

      unless new_value.nil?
      end

      @pending_verification = new_value
    end

    # Generates #hash and #== methods from all fields
    # #== @return [Bool]
    # #hash calculates hash code according to all attributes.
    # #hash @return [UInt64] Hash code
    def_equals_and_hash(@alternatives, @alternatives_present, @current_deadline, @current_deadline_present, @currently_due, @currently_due_present, @disabled_reason, @disabled_reason_present, @errors, @errors_present, @eventually_due, @eventually_due_present, @past_due, @past_due_present, @pending_verification, @pending_verification_present)
  end
end
