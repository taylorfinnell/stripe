#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2025-06-30.basil
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"


module Stripe
  # 
  class InvoiceSettingCustomerRenderingOptions
    include JSON::Serializable
    include JSON::Serializable::Unmapped
    include OpenApi::Validatable
    include OpenApi::Json


    # Optional Properties

    # How line-item prices and amounts will be displayed with respect to tax on invoice PDFs.
    @[JSON::Field(key: "amount_tax_display", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: amount_tax_display.nil? && !amount_tax_display_present?)]
    getter amount_tax_display : String? = nil
    MAX_LENGTH_FOR_AMOUNT_TAX_DISPLAY = 5000

    @[JSON::Field(ignore: true)]
    property? amount_tax_display_present : Bool = false


    # ID of the invoice rendering template to be used for this customer's invoices. If set, the template will be used on all invoices for this customer unless a template is set directly on the invoice.
    @[JSON::Field(key: "template", type: String?, default: nil, required: false, nullable: true, emit_null: true, presence: true, ignore_serialize: template.nil? && !template_present?)]
    getter template : String? = nil
    MAX_LENGTH_FOR_TEMPLATE = 5000

    @[JSON::Field(ignore: true)]
    property? template_present : Bool = false


    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(
      *,
      # Optional properties
      @amount_tax_display : String? = nil,
      @template : String? = nil
    )
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties : Array(String)
      invalid_properties = Array(String).new

      unless (_amount_tax_display = @amount_tax_display).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("amount_tax_display", _amount_tax_display.to_s.size, MAX_LENGTH_FOR_AMOUNT_TAX_DISPLAY)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_template = @template).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("template", _template.to_s.size, MAX_LENGTH_FOR_TEMPLATE)
          invalid_properties.push(max_length_error)
        end
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid? : Bool

      unless (_amount_tax_display = @amount_tax_display).nil?
        return false if _amount_tax_display.to_s.size > MAX_LENGTH_FOR_AMOUNT_TAX_DISPLAY
      end

      unless (_template = @template).nil?
        return false if _template.to_s.size > MAX_LENGTH_FOR_TEMPLATE
      end

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] amount_tax_display Object to be assigned
    def amount_tax_display=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("amount_tax_display", new_value.to_s.size, MAX_LENGTH_FOR_AMOUNT_TAX_DISPLAY)
      end

      @amount_tax_display = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] template Object to be assigned
    def template=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("template", new_value.to_s.size, MAX_LENGTH_FOR_TEMPLATE)
      end

      @template = new_value
    end

    # Generates #hash and #== methods from all fields
    # #== @return [Bool]
    # #hash calculates hash code according to all attributes.
    # #hash @return [UInt64] Hash code
    def_equals_and_hash(@amount_tax_display, @amount_tax_display_present, @template, @template_present)
  end
end
