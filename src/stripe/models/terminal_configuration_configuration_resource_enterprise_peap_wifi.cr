#  Stripe API
#
# The Stripe REST API. Please see https://stripe.com/docs/api for more details.
#
# The version of the OpenAPI document: 2025-06-30.basil
# Contact: dev-platform@stripe.com
# Generated by: https://openapi-generator.tech
#

require "../../core"


module Stripe
  # 
  class TerminalConfigurationConfigurationResourceEnterprisePeapWifi
    include JSON::Serializable
    include JSON::Serializable::Unmapped
    include OpenApi::Validatable
    include OpenApi::Json

    # Required Properties

    # Password for connecting to the WiFi network
    @[JSON::Field(key: "password", type: String?, default: nil, required: true, nullable: false, emit_null: false)]
    getter password : String? = nil
    MAX_LENGTH_FOR_PASSWORD = 5000



    # Name of the WiFi network
    @[JSON::Field(key: "ssid", type: String?, default: nil, required: true, nullable: false, emit_null: false)]
    getter ssid : String? = nil
    MAX_LENGTH_FOR_SSID = 5000



    # Username for connecting to the WiFi network
    @[JSON::Field(key: "username", type: String?, default: nil, required: true, nullable: false, emit_null: false)]
    getter username : String? = nil
    MAX_LENGTH_FOR_USERNAME = 5000


    # End of Required Properties

    # Optional Properties

    # A File ID representing a PEM file containing the server certificate
    @[JSON::Field(key: "ca_certificate_file", type: String?, default: nil, required: false, nullable: false, emit_null: false)]
    getter ca_certificate_file : String? = nil
    MAX_LENGTH_FOR_CA_CERTIFICATE_FILE = 5000



    # Initializes the object
    # @param [Hash] attributes Model attributes in the form of hash
    def initialize(
      *,
      # Required properties
      @password : String? = nil, 
      @ssid : String? = nil, 
      @username : String? = nil,
      # Optional properties
      @ca_certificate_file : String? = nil
    )
    end

    # Show invalid properties with the reasons. Usually used together with valid?
    # @return Array for valid properties with the reasons
    def list_invalid_properties : Array(String)
      invalid_properties = Array(String).new

      invalid_properties.push("\"password\" is required and cannot be null") if @password.nil?

      unless (_password = @password).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("password", _password.to_s.size, MAX_LENGTH_FOR_PASSWORD)
          invalid_properties.push(max_length_error)
        end
      end
      invalid_properties.push("\"ssid\" is required and cannot be null") if @ssid.nil?

      unless (_ssid = @ssid).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("ssid", _ssid.to_s.size, MAX_LENGTH_FOR_SSID)
          invalid_properties.push(max_length_error)
        end
      end
      invalid_properties.push("\"username\" is required and cannot be null") if @username.nil?

      unless (_username = @username).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("username", _username.to_s.size, MAX_LENGTH_FOR_USERNAME)
          invalid_properties.push(max_length_error)
        end
      end
      unless (_ca_certificate_file = @ca_certificate_file).nil?
        if max_length_error = OpenApi::PrimitiveValidator.max_length_error("ca_certificate_file", _ca_certificate_file.to_s.size, MAX_LENGTH_FOR_CA_CERTIFICATE_FILE)
          invalid_properties.push(max_length_error)
        end
      end
      invalid_properties
    end

    # Check to see if the all the properties in the model are valid
    # @return true if the model is valid
    def valid? : Bool

      return false if @password.nil?
      unless (_password = @password).nil?
        return false if _password.to_s.size > MAX_LENGTH_FOR_PASSWORD
      end

      return false if @ssid.nil?
      unless (_ssid = @ssid).nil?
        return false if _ssid.to_s.size > MAX_LENGTH_FOR_SSID
      end

      return false if @username.nil?
      unless (_username = @username).nil?
        return false if _username.to_s.size > MAX_LENGTH_FOR_USERNAME
      end

      unless (_ca_certificate_file = @ca_certificate_file).nil?
        return false if _ca_certificate_file.to_s.size > MAX_LENGTH_FOR_CA_CERTIFICATE_FILE
      end

      true
    end

    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] password Object to be assigned
    def password=(new_value : String?)

      raise ArgumentError.new("\"password\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("password", new_value.to_s.size, MAX_LENGTH_FOR_PASSWORD)
      end

      @password = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] ssid Object to be assigned
    def ssid=(new_value : String?)

      raise ArgumentError.new("\"ssid\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("ssid", new_value.to_s.size, MAX_LENGTH_FOR_SSID)
      end

      @ssid = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] username Object to be assigned
    def username=(new_value : String?)

      raise ArgumentError.new("\"username\" is required and cannot be null") if new_value.nil?
      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("username", new_value.to_s.size, MAX_LENGTH_FOR_USERNAME)
      end

      @username = new_value
    end
    # Custom attribute writer method checking allowed values (enum).
    # @param [Object] ca_certificate_file Object to be assigned
    def ca_certificate_file=(new_value : String?)

      unless new_value.nil?
        OpenApi::PrimitiveValidator.validate_max_length("ca_certificate_file", new_value.to_s.size, MAX_LENGTH_FOR_CA_CERTIFICATE_FILE)
      end

      @ca_certificate_file = new_value
    end

    # Generates #hash and #== methods from all fields
    # #== @return [Bool]
    # #hash calculates hash code according to all attributes.
    # #hash @return [UInt64] Hash code
    def_equals_and_hash(@password, @ssid, @username, @ca_certificate_file)
  end
end
